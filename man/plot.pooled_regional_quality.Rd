% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.pooled_regional_quality.R
\name{plot.pooled_regional_quality}
\alias{plot.pooled_regional_quality}
\title{Plot Regional Immunization Data Quality Trends}
\usage{
\method{plot}{pooled_regional_quality}(x, ...)
}
\arguments{
\item{x}{A data frame of class \code{pooled_regional_quality} containing
summarized regional immunization data quality.}

\item{...}{Additional arguments passed to \code{ggplot()}, if any.}
}
\value{
A \code{ggplot} object representing immunization data quality trends over time.
}
\description{
This function generates a line plot visualizing trends in regional
immunization data quality indicators over time. The function is designed for
data frames of class \code{pooled_regional_quality}, typically produced by
\code{process_regional_quality_data()}.
}
\details{
The plot displays trends for three key immunization data quality metrics:
\itemize{
\item \strong{Overall Data Quality (\code{mean_overall_dqa})}: General assessment of data quality.
\item \strong{Vaccination-Only Data Quality (\code{mean_overall_vacc_only})}: Data quality for reported vaccines only.
\item \strong{Tracer Vaccine Data Quality (\code{mean_overall_vacc_tracer})}: Data quality for tracer vaccines.
}
}
\examples{
# Sample dataset
df <- data.frame(
  year = rep(2015:2022, each = 3),
  mean_overall_dqa = runif(24, 60, 100),
  mean_overall_vacc_only = runif(24, 50, 95),
  mean_overall_vacc_tracer = runif(24, 40, 90)
)

# Assign class to match expected input type
class(df) <- c("pooled_regional_quality", class(df))

# Generate the quality plot
plot.pooled_regional_quality(df)

}
