% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.pooled_dropout.R
\name{plot.pooled_dropout}
\alias{plot.pooled_dropout}
\title{Plot Dropout Heatmap for Pooled Dropout Data}
\usage{
\method{plot}{pooled_dropout}(x, ...)
}
\arguments{
\item{x}{A data frame of class \code{pooled_coverage} containing processed dropout data.}

\item{...}{Additional arguments passed to \code{ggplot()}, if any.}
}
\value{
A \code{ggplot} object representing the dropout heatmap.
}
\description{
This function generates a heatmap visualization for dropout rates categorized
into predefined groups. The function is specifically designed for objects of class
\code{pooled_coverage}, which are created using \code{process_dropout_data()}.
}
\details{
The heatmap represents dropout categories with colors, where higher dropout percentages
are shown in dark red and lower percentages in green or yellow.
}
\examples{
# Sample dataset
df <- data.frame(
  year = c(2020, 2021, 2021, 2022),
  country = c("A", "B", "A", "B"),
  adminlevel_1 = c("Region1", "Region2", "Region1", "Region2"),
  dropout_1 = c(-12, 8, -4, 11)
)

# Process the dropout data
processed_df <- process_dropout_data(df, "dropout_1")

# Generate the dropout heatmap
plot(processed_df)

}
